Resolver con PMA (Pasaje de mensajes Asincronicos) el siguiente problema. En una clase
de la materia hay 3 ayudantes que deben responder consultas de A alumnos de acuerdo al orden de llegada. Cada alumno cuando llega espera a que cualquier ayudante le responda la consulta y se retira(hace una sola consulta). Cada vez que un ayudante esta libre aprovecha a corregir un parcial(hasta no terminarlo no puede atender consultas). Nota: maximiizar la concurrencia y no generar demora innecesaria
_________________________________________________________________________
COMO RESOLVERLO:
como vemos en el enunciado vamos a tener un proceso de 1 a 3 ayudantes, 
otro proceso para los A alumnos y  un proceso Administrador para coordinar a los ayudantes cuando estan libres para dar consultas
___________________________________________________________________
//declaracion de canales
chan solicitarConsulta (int id,string "consulta");
chan seRetira(int id);
chan Ayudantelibre (int ayu);chan contestarConsulta[idA](int idAyu)
chan contestarCorregirParcial(int idAyu)

process Alumno [idA: 0..n-1]{
send solicitarConsulta (idA, "consulta");  //enviar solicitud de consulta
//realizar consulta 
receive seRetira[id](numF);     // recibio el msj que se retira el alumno
}

process Ayudante [idAyu: 1..3]{
var 
      int idA;
while(true){
 send Ayudantelibre (int idAyu);  //enviar el msj que el ayudante esta libre
 if (notEmpty(contestarCorregirParcial(int idAyu)))->  si el canal de corregir parcial no esta vacio
         receive contestarCorregirParcial(int idAyu)  //recibo el msj corregir parcial
  [] (Empty(contestarCorregirParcial(int idAyu)))-> // si el canal de corregir parcial esta vacio
         receive contestarConsulta[idA](int idAyu) //recibo el msj corregir parcial
}
}

process Administrador{
var
    int idA,idAyu;
while(true)
{
    receive solicitarConsulta (idA, "consulta");  //recibo el msj de solicitar consulta
    receive Ayudantelibre (int idAyu);    //recibo el msj de ayudante libre
    send contestarCorregirParcial(int idAyu)  //envio el msj corregir parcial
    send contestarConsulta[idA](int idAyu)   //envio el msj contestar consulta
}
}
